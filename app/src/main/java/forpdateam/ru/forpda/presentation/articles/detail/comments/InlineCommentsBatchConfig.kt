package forpdateam.ru.forpda.presentation.articles.detail.comments

/** UI batch size for inline news comments (initial expand + each scroll/page load). */
object InlineCommentsBatchConfig {
    const val BATCH_SIZE = 20
}
